function App() {
  const [counter , setCounter] = useState(0);
  return (
    <div className="w-full flex items-center justify-center bg-red-900">
        <h1 className="btn btn-success" onClick={() => {setCounter((prev) => {return prev+1})}}>{counter}</h1>
    </div>
  )
}

