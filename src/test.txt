function App() {

  function add(x, y) {
      return x + y;
  }

  function mul(x, y) {
      return add(x + y, x - y) * add(x, y);
  }

 console.log("Hey I am here");
 const result = mul(5, 10) ;
 const addResult = add(5, 10);

  if (result > 0) {
      console.log("it is greater");
  } else if (addResult < 0) {
      console.log("it is not grater")
  } else {
      console.log("Didn't make it to here");
  }

  console.log("the end")

  return (
    <div>
    </div>
  )
}

